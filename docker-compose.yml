version: "3.8"
volumes:
  src:
    driver_opts:
      type: none
      device: $PWD/server
      o: bind
services:
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    volumes:
      - src:/go/src/app
    ports:
      - 5000:5000
